import { Link } from 'react-router-dom';

const CafeteriaCard = ({ cafeteria }) => {
  return (
    <div>
      {/* TODO: Build your cafeteria card UI */}
    </div>
  );
};

export default CafeteriaCard;
